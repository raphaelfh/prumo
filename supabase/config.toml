project_id = "supabase_local"

# Edge Functions have been migrated to FastAPI.
# See backend/app/api/v1/endpoints/ for the new implementations:
#   - ai_assessment.py (replaces ai-assessment Edge Function)
#   - section_extraction.py (replaces section-extraction Edge Function)
#   - model_extraction.py (replaces model-extraction Edge Function)
#   - zotero_import.py (replaces zotero-import Edge Function)
