// TextLayer.tsx — stub for Phase 2.
// Full implementation lands with the search plugin (next dispatch).
// Text selection and highlight support require pdfjs.renderTextLayer()
// wiring which is deferred to keep this dispatch focused on core render flow.
export function TextLayer({pageNumber: _pageNumber}: {pageNumber: number}) {
  return null;
}
