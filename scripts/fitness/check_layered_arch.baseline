# Layered-architecture violations on the current tree, grandfathered.
# Format: <repo-relative-file>:<full-import-module>  # why it is grandfathered
# Line-independent. May shrink, never grow: fix the edge, delete the line.
backend/app/schemas/hitl_session.py:app.models.extraction_versioning  # re-exports the TemplateKind ORM enum so endpoints avoid app.models, which launders the api->models ban; fix by moving TemplateKind to app.domain
