# Project structure (macro)

> This file is now a stub. For the structural overview of the
> extraction + HITL stack — schema, services, run lifecycle, QA seeds —
> see the canonical reference:

**👉 [`docs/architecture/extraction-hitl-architecture.md`](../architecture/extraction-hitl-architecture.md)**

For day-to-day commands and AI-assistant rules, see:

- **Root [`CLAUDE.md`](../../CLAUDE.md)** — recent changes, active
  technologies, command cheat sheet.
- **[`.claude/CLAUDE.md`](../../.claude/CLAUDE.md)** — project guide for AI
  assistants (db-migration rules, seeding, key file locations).
- **`Makefile`** — `make setup`, `make start`, `make stop`,
  `make test-backend`, `make lint-backend`.
